void test() {
    /*
	t_new_pokemon* newPokemon = malloc(sizeof(t_new_pokemon));
	newPokemon->nombre_pokemon = string_new();
	string_append(&newPokemon->nombre_pokemon, "PIKACHU");

	newPokemon->cantidad = 10;
	newPokemon->pos_x = 2;
	newPokemon->pos_y = 5;
	createNewPokemon(newPokemon);

	t_catch_pokemon* catchPokemon = malloc(sizeof(t_catch_pokemon));
	catchPokemon->nombre_pokemon = string_new();
	string_append(&catchPokemon->nombre_pokemon, "PIKACHU");

	catchPokemon->pos_x = 2;
	catchPokemon->pos_y = 5;

	catchAPokemon(catchPokemon);
	*/

	//catchAPokemon(catchPokemon);
	//createNewPokemon(newPokemon11);
	// t_list* list = getAPokemon(getPokemon);
	// printListOfPokemonReadedLines(list);
	//game_card_logger_info("Termino todo OK");
}